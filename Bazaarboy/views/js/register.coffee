@Bazaarboy.user.register =
    init: =>
        $(document).ready =>
            $("form[name=register]").submit (event) =>
                event.preventDefault()
                return
            return
        return