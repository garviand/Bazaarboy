//- Components.Top

div.logo
    a(href="{% url index %}")
        div.left
            img(src="{{ STATIC_URL }}images/logo.png")
        div.right
            b Bazaarboy
        div.clear
div.controls
    {% if user %}
    div.name
        a {{ user.full_name }}
    div.create_event.user_create_event
        a(href="javascript:;", data-id="{{ user.id }}") Publish an Event
    {% else %}
    div.create_event
        a(href="{% url user:register %}") Create an Event
    {% endif %}
    {% if user %}
    div.logout
        a(href="{% url user:logout %}") Log Out
    {% else %}
    div.login
        a(href="{% url index %}") Log In
    {% endif %}
    div.clear
div.clear